{{ form_start(form) }}
<div class="row">
	<div class="col-md-3">{{ form_row(form.showingDate) }}</div>
	<div class="col-md-3">{{ form_row(form.price) }}</div>
	<div class="col-md-2">{{ form_row(form.endAt) }}</div>
</div>
<button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
